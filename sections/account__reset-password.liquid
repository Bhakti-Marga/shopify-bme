<section class="account self-center justify-self-center w-full max-w-[475px]">
  <div class="account__container border">
    {%- form 'reset_customer_password', class: 'account__reset-password' -%}
      <h1 class="account__title">Restablecer contraseña de cuenta</h1>
      <p>Introduce una nueva contraseña para {{email}}</p>
      {%- if form.errors -%}
        <ul class="form__errors">
          {% for field in form.errors %}
            <li>
              {% if field == 'form' %}
                {{ form.errors.messages[field] }}
              {% else %}
                {{ form.errors.translated_fields[field] }} - {{ form.errors.messages[field] }}
              {% endif %}
            </li>
          {% endfor %}
        </ul>
      {%- endif -%}

      <div class="relative">
        <label for="password" class="form__label">
          Contraseña
        </label>
        <input
          type="password"
          name="customer[password]"
          id="password"
          class="form__element"
          autocomplete="new-password"
          {% if form.errors contains 'password' %}
            aria-invalid="true"
            aria-describedby="password-error"
          {% endif %}
          placeholder="Contraseña"
        >
      </div>

      <div class="relative">
        <label for="password_confirmation" class="form__label">
          Confirmar contraseña
        </label>
        <input
          type="password"
          name="customer[password_confirmation]"
          id="password_confirmation"
          class="form__element"
          autocomplete="new-password"
          {% if form.errors contains 'password_confirmation' %}
            aria-invalid="true"
            aria-describedby="password_confirmation-error"
          {% endif %}
          placeholder="Confirmar contraseña"
        >
      </div>

      <button class="btn">Restablecer contraseña</button>
    {%- endform -%}
  </div>
</section>
